"""DAWorkbench statistical plotting package.

This package provides seaborn-style statistical plot functions that compute
in Python (numpy / scipy / pandas) and render through the embedded ``da_figure``
module (Qwt 7.3.4). Every submodule exposes a ``plot(df, column, chart, **params)``
entry point following the convention documented in ``plan/README.md``.
"""
